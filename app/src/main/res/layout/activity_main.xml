<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Search Bar -->
    <EditText
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:drawableStart="@android:drawable/ic_menu_search"
        android:hint="Search"
        android:padding="10dp"
        android:background="@drawable/search_bar_background" />

    <!-- Ads and Offers -->
    <TextView
        android:id="@+id/ads_offers_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_bar"
        android:layout_margin="16dp"
        android:text="Ads and Offers"
        android:textSize="18sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/ads_offers_image"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/ads_offers_title"
        android:layout_margin="16dp" />

    <!-- Recommended Shops -->
    <TextView
        android:id="@+id/recommended_shops_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ads_offers_image"
        android:layout_margin="16dp"
        android:text="Recommended Shops"
        android:textSize="18sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recommended_shops_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_below="@id/recommended_shops_title"
        android:layout_margin="16dp"
        android:orientation="horizontal" />

    <TextView
        android:id="@+id/no_shops_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No shops found"
        android:textSize="18sp"
        android:visibility="gone"
        android:layout_below="@id/recommended_shops_title"
        android:textAlignment="center" />

    <!-- Recommended Barbers -->
    <TextView
        android:id="@+id/recommended_barbers_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/recommended_shops_recycler_view"
        android:layout_margin="16dp"
        android:text="Recommended Barbers"
        android:textSize="18sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recommended_barbers_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_below="@id/recommended_barbers_title"
        android:layout_margin="16dp"
        android:orientation="horizontal" />

    <TextView
        android:id="@+id/no_barbers_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No barbers found"
        android:textSize="18sp"
        android:visibility="gone"
        android:layout_below="@id/recommended_barbers_title"
        android:textAlignment="center" />

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?android:attr/windowBackground"
        app:menu="@menu/bottom_navigation_menu" />

</RelativeLayout>
